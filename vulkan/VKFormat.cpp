#include "VKFormat.h"

namespace vk
{
	VkFormat ImageFormatToVkFormat(ImageFormat format)
	{
		return VkFormat();
	}

	ImageFormat VkFormatToImageFormat(VkFormat format)
	{
		return ImageFormat();
	}
}